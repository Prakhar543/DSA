#include <iostream>

using namespace std;
int main(){
    
    /*Segmentation fault occurs mostly when there is not enough memory for the program to run
    Or during runtime the program is using excess memory then allocation to it.*/

    return 0;
}